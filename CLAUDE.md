# LicoMesh

Repository agent guidance lives in the lico-dev skill `lico-mesh-repository`.

## 项目开发规范

- 严禁泄露本机信息、个人隐私、密钥密文，涉及后端服务时应严格禁止泄露运行数据。
- 涉及算法和数据结构的任务应对比学习开源框架的优秀代码，尽可能应用最佳实践，降低算法复杂度，选择最优的数据结构和缓存策略，减少重复计算，优化调度分派，降低内存开销，提升并发性能。
- 涉及重构的任务必须一次性完整迁移，严禁残留旧实现、旧兼容。已被移除的代码或文档应用一次性脚本验证，严禁长期占用测试用例或门禁检查。
- 项目开发应该以一个最小可独立验收的功能、模块或场景为闭环目标，一次改动严禁横跨多个不同的功能、模块或场景。开发过程中应仅实施必要改动，快速闭环后执行最小测试，之后再执行下一个功能、模块或场景的改造，以避免开发过程中逐渐扩大修改范围导致回归测试负担过重。
- 涉及回归测试，应尽可能采取较快闭环的路线，减少回归测试的范围。仅在所有改动均实施且验证有效之后才可以执行完整的回归测试，项目过程中严禁多次执行全量回归，应尽可能避免影响其它智能体的并行开发工作。

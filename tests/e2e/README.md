# End-to-End Tests

MCP, external-service, verified-plugin-package, and Docker flows.

- MCP gateway install → register → discover → uninstall flow
- Verified plugin-package install, activation, contribution, and cleanup
- External service integration (Docker-based)
- Full server startup → console → agent workflow

#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
export NODE_OPTIONS="${NODE_OPTIONS:+$NODE_OPTIONS }--conditions=source"
node "$repo_root/tools/scripts/verify-git-publication.ts" --staged

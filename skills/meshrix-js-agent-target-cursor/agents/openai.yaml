interface:
  display_name: "Agent Target Cursor"
  short_description: "Reference cursor 适配项: forms, paths, sessions, access"
  default_prompt: "Use $meshrix-js-agent-target-cursor as reference for cursor 适配项 (forms, paths, config, sessions, databases, access, operations, consumers)."
